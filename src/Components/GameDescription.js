import React from "react";

function GameDescription() {
    return (
        <>
            <h1>Memory Game</h1>
            <p>Click on the cards to flip them and find the <br/>
                matching pairs with as few flips as possible.</p>
        </>
    );
}

export default GameDescription;
